import { ResizebleDirective } from './resizeble.directive';

describe('ResizebleDirective', () => {
  it('should create an instance', () => {
    const directive = new ResizebleDirective();
    expect(directive).toBeTruthy();
  });
});
